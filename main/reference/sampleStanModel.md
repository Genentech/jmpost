# `sampleStanModel`

Sample from a Stan Module.

## Usage

``` r
sampleStanModel(object, ...)

# S3 method for class 'JointModel'
sampleStanModel(object, data, ...)
```

## Arguments

- object:

  the module.

- ...:

  additional arguments.

- data:

  (`DataJoint` or `list`)\
  input data.
