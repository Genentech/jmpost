# Extract shrinkage factors from a `JointModelSamples` object

Extract shrinkage factors from a `JointModelSamples` object

## Usage

``` r
# S3 method for class 'JointModelSamples'
shrinkage(object, ...)
```

## Arguments

- object:

  (`JointModelSamples`) the object to extract shrinkage factors from.

- ...:

  not used.

## Value

the shrinkage factors correctly named after the covariate names.

## See also

Other shrinkage:
[`shrinkage()`](https://genentech.github.io/jmpost/reference/shrinkage.md)
