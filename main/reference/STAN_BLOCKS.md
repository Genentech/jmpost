# List of Stan Blocks

A list with 1 element per standard Stan program blocks. This object is
mostly used internally as a reference for what blocks are expected to
exist within a given Stan program.

## Usage

``` r
STAN_BLOCKS
```

## Format

An object of class `list` of length 7.
